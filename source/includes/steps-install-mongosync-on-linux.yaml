# This is a parent procedure that provides steps for other procedures to inherit.
# This procedure is NOT intended for direct inclusion in a tutorial.
title: Download the tarball.
stepnum: 1
level: 4
ref: download-mongosync-linux
pre: |
  After you have installed the required prerequisite packages, download
  the Cluster-to-Cluster Sync ``.tgz`` tarball from the following link:

  |arrow| |mdb-download-link|

  a. In the :guilabel:`Version` dropdown, select the version of
     Cluster-to-Cluster Sync to download.

  #. In the :guilabel:`Platform` dropdown, select your operating system
     version and architecture.

  #. In the :guilabel:`Package` dropdown, select :guilabel:`shell (tgz)`.

  #. Click :guilabel:`Download`.
---
title: Extract the files from the downloaded archive.
stepnum: 2
level: 4
ref: extract
pre: |
  For example, from a system shell, you can extract using the ``tar`` command:

  .. code-block:: bash

     tar -zxvf mongosync-*.tgz
---
title: Ensure the binary is in a directory listed in your ``PATH`` environment variable.
stepnum: 3
level: 4
ref: c2c-ensure-binary-in-path
content: |
  The ``mongosync`` binary is in the ``dist/`` directory of the tarball.
  You can either:

  - Copy the binary into a directory listed in your ``PATH``
    variable, such as ``/usr/local/bin`` (Update
    ``/path/to/mongosync/dist`` with your installation
    directory as appropriate)

    .. code-block:: bash

       sudo cp /path/to/mongosync/dist/* /usr/local/bin/

  - Create symbolic links to the binaries from a directory listed in
    your ``PATH`` variable, such as ``/usr/local/bin`` (Update
    ``/path/to/mongosync/dist/`` with your installation
    directory as appropriate):

    .. code-block:: bash

       sudo ln -s  /path/to/mongosync/dist/* /usr/local/bin/
