stepnum: 1
ref: download-mongosync-macos
level: 4
title: Download the tarball.
pre: |
  Download the Cluster to Cluster Sync ``tgz`` tarball from the following
  link:

  |arrow| |mdb-download-link|

  a. In the :guilabel:`Version` dropdown, select the version of
     Cluster to Cluster Sync to download.

  #. In the :guilabel:`Platform` dropdown, select :guilabel:`macOS`.

  #. In the :guilabel:`Package` dropdown, select :guilabel:`tgz`.

  #. Click :guilabel:`Download`.
---
stepnum: 2
level: 4
source:
  file: steps-install-mongosync-on-linux.yaml
  ref: extract
# The content of the following include is autogenerated:
pre: |
    .. include:: /includes/release/untar-release-ent-osx-x86_64.rst

    If your web browser automatically unzips the file as part of the 
    download, the file would end in ``.tar`` instead.
---
stepnum: 3
level: 4
source:
  file: steps-install-mongosync-on-linux.yaml
  ref: ensure-binaries-in-path
...