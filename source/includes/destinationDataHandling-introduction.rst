Starting in ``mongosync-beta`` 1.8, use the ``destinationDataHandling``
option to define the behavior for destination cluster databases.
Previous ``mongosync`` versions return an error if the destination
cluster is empty.
