.. Verifier Guardrail Notice

If you need to restart ``mongosync`` during a commit, such as if
``mongosync`` crashes in the ``WaitingForCommitComplete`` phase,
you must restart ``mongosync`` with the verifier disabled.





